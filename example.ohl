private int factorial(n: int) {
    if (n < 2) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}

public null main() {
    let result: int = factorial(5);
    print(result);
}