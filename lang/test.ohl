public int main() {
    int Num = 5;
    foo();
    return 0;
}

public null foo() {
    int x = 4;
    boolean even = isEven(x);
    print(even);
}

public boolean isEven(int x) {
    int rem = x % 2;
    if (rem == 0) return true;

    return false;
}
